<h1></h1>
<p>Find me in app/views/lists/show.html.erb</p>

<%= link_to "Back to list", lists_path %>
<%= link_to 'detete this list', list_path(@list), method: :delete, data: { confirm: "Sûr ???"} %>

<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://source.unsplash.com/collection/39769052/1600x900);">
  <div class="container">
    <h1><%= "You haven't saved any movies yet!" if @list.movies.empty? %></h1>
    <h1><%= @list.name %></h1><br>
    <a class="btn btn-flat"<%= link_to "Add a movie", new_list_bookmark_path(@list) %></a>
  </div>
</div>

<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
    <div class="row my-2">
      <% @list.movies.each do |movie| %>
        <div class="col-12 col-sm-6 mb-4 card-movie">
          <% bookmark = Bookmark.find_by(list: @list, movie: movie) %><br>
          <%= image_tag movie.poster_url, alt: "#{movie.title} picture" %><br>
          <div class="card-movie-infos">
            <%= movie.overview %><br>
            <div class="card-movie-comment">
              <i><%= bookmark.comment %></i><br>
            </div>
            <%= link_to 'delete', bookmark_path(bookmark), method: :delete, data: { confirm: "Are you sure you want to remove #{movie.title} from your #{@list.name} list"} %>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>



